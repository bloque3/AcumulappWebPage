---
import { Image } from 'astro:assets';
import phonePreview from '../assets/phone-app-preview-3.webp'
import phonePreview_dark from '../assets/phone-app-preview-4.webp'

var BASE_URL = import.meta.env.BASE_URL ;
if(BASE_URL != '/'){
  BASE_URL += '/';
}
---
<section class="relative pt-32 pb-20 md:pt-32 overflow-hidden hero-section">
  <div class="container-custom relative z-10">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <div class="slide-up" transition:animate="slide">
        <h1 class="mb-6 text-secondary-900 dark:text-white leading-tight">
          La <span class="text-primary-600 dark:text-primary-400">App</span> de fidelización <br>
          definitiva para tu negocio
        </h1>
        <p class="text-xl text-secondary-600 dark:text-secondary-300 mb-8 max-w-lg">
          Acumulapp es la solución perfecta para gestionar programas de fidelización y recompensas, diseñada para impulsar la lealtad de tus clientes y aumentar las ventas de tu negocio.
        </p>
        <div class="flex flex-wrap gap-4">
          <a href=`${BASE_URL}download` class="btn-primary">
           Descarga la App 
          </a>
          <a href=`${BASE_URL}contact`  class="btn-outline flex items-center">
           Contactanos 
          </a>
        </div>
      </div>
      
      <div class="relative fade-in " transition:animate="fade">
        <!-- Animated Elephant SVG Hero Image -->
        <div class="w-full max-w-sm mx-auto  relative hover:scale-103 transition duration-300 relative">
          <Image
            src={phonePreview}
            alt="app phone preview light mode"
            class="w-full h-auto object-cover dark:hidden z-10"
            loading="lazy" />

          <Image
            src={phonePreview_dark}
            alt="app phone preview dark mode"
            class="w-full h-auto object-cover hidden z-10 dark:block"
            loading="lazy" />
        </div>
      </div>
    </div>
  </div>
  
  <!-- Teal section at the bottom with Trusted By -->
  <div class="mt-10 w-full overflow-hidden -z-10" transition:animate="slide">
    <div class="relative bg-primary-600 dark:bg-primary-400 py-12 md:py-16">
      <div class="container-custom">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <div>
            <div class="text-3xl md:text-4xl font-bold text-black dark:text-black">
              Fidellización <span class="bg-accent-400 px-2 py-1">digital</span> resultados reales
            </div>
          </div>
          
          <div class="flex flex-col items-start md:items-end justify-center">
            <!--Espacio para logos de empresas que confian en nuestros servicios, en un carrusel-->
            <!--<p class="text-sm uppercase tracking-wider text-primary-950 dark:text-primary-950 font-medium mb-4">TRUSTED BY</p>-->
            <!--<div class="flex flex-wrap justify-start md:justify-end gap-x-8 gap-y-4">-->
            <!--  <div class="h-8 flex items-center">-->
            <!--    <span class="text-black dark:text-black font-medium">El Chorro</span>-->
            <!--  </div>-->
            <!--</div>-->
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Add keyframes for additional animations if needed */
  @keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0px); }
  }
  
  /* Apply floating animation to the entire SVG container */
  .fade-in svg {
    animation: float 6s ease-in-out infinite;
  }
</style>
